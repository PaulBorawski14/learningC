#include <stdio.h>

/* MAKES AN INT. */
int main(int argc, char *argv[])
{
    //set int distance to 100
    int distance = 100;

    // prints
    printf("You are %d miles away.\n", distance);

    return 0;
}
